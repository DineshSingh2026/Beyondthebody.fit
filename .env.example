# Copy to .env and fill in your values.
# DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@localhost:5432/beyond_the_body
# (If password contains @, encode it as %40 in the URL)

# Optional: override for E2E and check scripts
# BASE_URL=http://localhost:3000
# PORT=3000
